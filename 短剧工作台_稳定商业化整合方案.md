# 短剧工作台 · 稳定商业化整合方案

> 日期：2026-08-11 ｜ 依据：AGNES 官网三文档 + Pavo 平台流程调研 + 现有稳定版（已存档 `_archive_20260811_stable_v1/`）
> 目标：**稳定商业化的出视频**——先保证"一句话/一段小说 → 可发布成片"闭环稳定跑通，再逐步对齐官方模式与 Pavo 交互。

---

## 一、我们 vs Pavo 流程对照（现状核验）

| Pavo 步骤（官方流程） | 我们现状 | 覆盖 |
|---|---|---|
| 1 输入一句话创意 | ① 创意·需求卡（贴小说 / 一句话主题 双入口） | 🟢 有 |
| 2 需求卡片确认（标题/概梗/时长/画幅/分镜模式/风格） | ① reqCard：标题/类型/时长/拆镜数/角色/风格，可编辑可确认 | 🟢 有 |
| 3 角色/场景/道具设计 + 自然语言定向改 | ③ 资产设计：三 tab + cn_prompt 中文描述 + 重生成 | 🟢 有 |
| 4 分镜故事板确认 | ② 大纲 + ④ 分镜校准·关键帧（首尾针两栏） | 🟢 有 |
| 5 关键帧 + 逐镜视频 | ④ 首尾针 + ⑤ 分镜视频（提交+轮询，防 524） | 🟢 有（首尾针 2 图更强） |
| 6 指定镜局部返工（自然语言） | ⑤ 卡片「✎ 返工」按钮（reworkShot） | 🟢 有 |
| 7 合成成片 → 下载 MP4 | ⑥ 合并成片（转场/合成/健康卡）+ 导出/下载 | 🟢 有 |
| **每步「生成→检查→确认→下一步」闸门** | confirmedSteps 闸门链 + 满意门禁（全部满意才放行合成） | 🟢 有 |
| 素材内部流转（图直接供后续调用） | 素材库引用 + 角色锚点云端复用 | 🟢 有 |

**结论：流程闭环已 100% 对齐 Pavo 骨架，且关键帧双图、质检健康卡、分镜级转场是超配。** 不需要推倒重来，只需要在"官方模式适配 + 交互顺滑 + 稳定加固"三处增强。

---

## 二、Pavo 流程的设计精髓（我们要吸收的 4 条）

1. **闸门式流程**：每步"生成 → 检查/微调 → 确认 → 下一步"，用户始终知道在哪、下一步干嘛 —— 我们已有（闸门链 + 满意门禁）。
2. **自然语言修改 > 专业参数**：Pavo 改角色用"猫耳收小一半，贴头皮"；返工用"2、3 镜重做，衔接不上"。我们已有 reworkShot，但**资产设计（③）仍是改 cn_prompt 专业字段**——可加"描述怎么改"快捷入口。
3. **底部主按钮驱动**：Pavo 是"生成全部镜头视频 → 合成短片 → 下载 MP4"三步大按钮，流程感强。我们各模块有批量按钮，但模块顶部说明文案略散——可统一"主按钮 + 进度"表达。
4. **素材流转闭环**：角色/场景/道具图在内部流转，引用即用。我们素材库已支持，但 UI 上"当前镜用了哪张素材、来源链"展示可更显式（frameStartLabel 已有雏形）。

---

## 三、官方模式落地清单（分层）

### A. 提示词层（对齐官网三种视频模式）
| 模式 | 官方结构 | 落地动作 |
|---|---|---|
| 文生视频 | [主体]+[动作]+[场景]+[运镜]+[光线]+[风格] | 已对齐（分镜 schema 生成 video_prompt） |
| **图生视频** | "描述**什么该动 + 哪些保持稳定**" | 🆕 reference 分支追加 `while keeping the face and outfit consistent`（官方稳定性句） |
| 关键帧动画 | 描述过渡关系 | 已对齐（_transition_prompt） |
| **图生图（尾帧/首帧 img2img）** | [改什么]+[新风格]+[增删]+[**保留元素**] | 🆕 尾帧 prompt 已含 preserve 语义，首帧 img2img 可补"preserving composition" |

### B. 数据层（字段契约）
| 字段 | 来源 | 落地动作 |
|---|---|---|
| `shot.scene_type` | 分镜生成源头（模型输出） | 🆕 STORYBOARD_SYS schema 加 scene_type，替代事后规则分类 |
| `shot.video_size` / `seconds` | 视频响应 `size`/`metadata.size_mapping` | 🆕 生成后回读写回，合成阶段用真实尺寸 |
| `shot.seed` | 视频接口支持 fixed seed | 🆕 可选：批量重渲可复现 |

### C. 管线层
| 能力 | 落地动作 |
|---|---|
| **Thinking 模式**（agnes-2.0-flash `enable_thinking`） | 🆕 剧本/分镜生成质量提升（源头） |
| **多图合成双人锚点**（agnes-image 官方 multi-image composition） | 🆕 dialogue_2 镜：角色A+角色B 锚点合成双人同框首帧，官方同框方案 |
| **限速批控**（RPM=5 / 批间隔 12s） | 🆕 批量视频创建节流，防 429 |

### D. UI 层
| 能力 | 落地动作 |
|---|---|
| scene_type 徽章 | 🆕 卡片显示「独白/对话/动作」（源头生成后直接有） |
| 官方运镜预设 chips | 🆕 ⑤ 运镜输入框下一排官方 14 运镜一键选 |
| ③ 资产"自然语言修改"入口 | 🆕 资产卡片加"描述怎么改"→ 后端改写 cn_prompt/img_prompt |

---

## 四、稳定商业化执行顺序（先稳定，后增强）

**Phase 0 · 稳定版（现状即基线，先跑通出片）**
- 当前功能闭环完整，先以 `_archive_20260811_stable_v1` 为基线，完整跑一遍 ①→⑥ 出片验证。

**Phase 1 · 官方适配（低成本高收益，本周）**
1. 图生视频稳定性句（reference 分支，纯后端 5 行）
2. STORYBOARD_SYS 源头带 scene_type（模型输出字段，后端 schema + load 兼容）
3. scene_type 徽章 + 运镜预设 chips（UI，纯前端）
4. 视频尺寸回读写回（后端 result 解析）

**Phase 2 · 能力增强（下周起）**
5. Thinking 模式提升剧本/分镜生成（源头质量）
6. 多图合成双人锚点（对话镜同框方案）
7. 限速批控（批量生成节流）
8. ③ 资产自然语言修改入口

**Phase 3 · 商业化打磨**
9. 出片自动质检链（SSIM 闸门 + 健康卡 → 合成）
10. 导出规范（AI 标识叠加、格式规范、封面）

---

## 五、验收标准（稳定商业化的定义）

- [ ] 一句话/一段小说 → ①→⑥ 全流程可无人值守跑通，出 1080P 竖屏成片
- [ ] 每步有确认闸门，误操作不毁数据（已具备）
- [ ] 单镜失败可局部返工，不推倒重来（已具备）
- [ ] 生成等待有明确反馈（已具备：提交+轮询）
- [ ] 出片通过 health check（黑场/静音/静帧/内容覆盖，已具备）
