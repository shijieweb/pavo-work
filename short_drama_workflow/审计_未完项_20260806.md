# 短剧自动化工作流 · 未完项 / 优化审计（2026-08-06）

## 范围说明
已排除（老板拍板不做 / 延后）：P3 深度人脸(Yunet)、P4 商用 BGM 曲库、P5 真平台分发、O5 VPS、O4 看板手动、D4 接口鉴权、D3 Ardot 视觉稿。

## 结论（一句话）
活跃路径（`server.py` + `studio.html` + `scripts/{generate,voice,edit}`）**已功能完整并经 REAL 全链路验证，无遗留 TODO/FIXME**。当前"未完成"集中在两类：
1. **早期遗留 / 死代码**（看起来像入口，实为 TODO 占位，未接真实 AGNES 管线）
2. **活跃路径上的 2~3 个次要优化**（非阻断，可选）

---

## 一、遗留 / 死代码（建议清理或决定去留）

| 文件 | 现象 | 是否接活跃管线 | 建议 |
|---|---|---|---|
| `main.py` + `harness/*` | Pavo 风 CLI 编排器；`harness/base_stage.py:233` TODO「应调用 scripts/generate/gen_ep01.py」，stage 未接真实生成 | **否**（仅 main.py 自引 harness；server.py 不用） | 要么接真实管线、要么废弃，避免误用 |
| `scripts/topic/main.py` | :21 TODO「真正的爆款分析逻辑」 | 否 | 废弃 / 删 |
| `scripts/script/main.py` | :22 TODO「调 AGNES 文本模型」 | 否 | 废弃 / 删 |
| `scripts/storyboard/main.py` | :67 TODO「调 AGNES 图生图」 | 否 | 废弃 / 删 |
| `flows/analysis.py` | :230 Flow3 theme 模式「AI 扩写小说」占位待接 | 否 | 废弃 / 删 |

> 说明：活跃管线 `server.py` 里的 `analyze_topic_real` / `generate_script_real` / `generate_storyboard_from_novel` / `run_pipeline` 才是真实现，上述 `main.py` 从未被导入，是早期脚手架。

---

## 二、活跃路径上的次要优化（非阻断，可选）

| 项 | 现状 | 优化建议 | 优先级 |
|---|---|---|---|
| **O1 跨集锁脸 REAL 验证** | 仅 mock 验证（剧集库命中→0 出图、新角色→1 出图，逻辑对）；未真调 AGNES | 起 `REAL=1` 跑 2 集共享角色，确认真实跨集同脸 + 省额度 | 中（低风险，建议补） |
| **ui_shot 静态占位** | `scripts/generate/gen_ep01.py:95` UI/弹窗镜用参考图静帧替代真视频（零 AGNES 消耗，但观感廉价） | 给 UI 镜也生成短视频；或保留为省额度策略并标注 | 低 |
| **风格化角色漏检** | `face_qc` 用 haar，CG 角色常漏检→默认通过（P3 Yunet 才根治） | 阶段3 前可临时调 haar 参数 / 加 profile 级联兜底 | 低（归 P3 范畴） |
| **O2 通知远程化** | `scripts/edit/notify.py` 仅本地 jsonl + 可选 webhook + 桌面 toast，已留 `NOTIFY_WEBHOOK` 口子 | 填 webhook 即远程（企业微信 / 飞书 / 邮件）；非必须 | 低 |

---

## 三、已确认"不是缺口"（避免误报）

- **ep01 / ep02 的 MiniMax `voice_id` 已填真实音色**（`male-qn-jingying` / `male-qn-qingse` / `female-shaonv`）→ 配音已可商用，非占位。
- `do_assemble` 默认带 BGM（P1 治本）、P2 免费 KEY 自动切换、O3 队列进度条/耗时/失败原因 —— 均已完成并验证。
- `distribute` 本地打包（抖音/快手/视频号 mp4 + manifest + drawtext 水印）已 REAL 通过；仅"真上传平台"属 P5。
- `studio.html` 里的 `placeholder=` 全是 HTML 输入框占位文案，非代码缺陷。

---

## 给老板的下一步建议
1. **优先处理遗留死代码**：`main.py`/`harness`/`flows`/`scripts/{topic,script,storyboard}/main.py` 五个入口都是 TODO 占位且未被活跃管线使用 → 建议统一废弃或删除（零风险），消除"看起来能跑其实空壳"的混淆。
2. **次要优化按需要挑**：最值得补的是 O1 的 REAL 跨集验证（确认省额度且真同脸）。
3. 其余（ui_shot 真视频、haar 兜底、O2 webhook 远程）均为锦上添花，可排期或暂放。
