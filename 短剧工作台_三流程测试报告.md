# 三流程真实浏览器测试报告（2026-08-11）

## 测试目标
按老板要求：本轮测试**修改参数（横屏等）覆盖不同流程**，最少三轮，每轮只生成 2 镜视频，发现问题即修。

## 前置：新增「横屏 16:9」能力
老板点名的横屏模式此前不支持，本轮先实现：
- 设置模块「🎬 生成参数」扩展为 **分辨率（测试480p/正式720p）+ 画幅（竖屏9:16/横屏16:9）** 双开关，存项目 meta
- 后端 `_video_size()` 统一入口（环境变量可覆盖）；prompt 构图词随画幅切换（vertical 9:16 / horizontal 16:9）
- 引擎参数区尺寸动态显示

## 三流程设计（覆盖全部用户操控面）

| 流程 | 覆盖操控 | 结果 |
|---|---|---|
| **流程1 横屏全链路** | 切横屏 → 新建项目 → ①需求卡(AI) → ②大纲(thinking) → 分镜 → ③场景图 → ④关键帧2镜 → ⑤视频2镜 | ✅ **17/17** |
| **流程2 竖屏参数操控** | 切竖屏 → ⑤光线/风格下拉 + 运镜chips + 改台词 → finalPrompt 实时更新 → 保存并重渲(force) → ④改引用(角色→场景)策略切 reference → 空镜生成 → 幂等跳过 | ✅ **15/15** |
| **流程3 批量+失败+合成** | 无人值守默认路径 → 批量关键帧/视频2镜 → 失败镜999反馈明确 → 标记满意 → 合成 | ✅ **10/10** |

## 真实输出验证（AGNES 真实生成）

| 项 | 结果 |
|---|---|
| 横屏视频输出 | **832×448**（提交 854×480，官方归一化 16:9）✅ |
| 竖屏视频输出 | **448×832**（提交 480×854，官方归一化 480p 档）✅ |
| 测试档合成成片 | **480×854 · 1.9MB**（修复前是 1080×1920 · 9MB）✅ |
| video_size 尺寸回读 | 全部联动写回 ✅ |
| 幂等跳过 | 重提已生成镜 → skipped（8ms 不烧额度）✅ |

## 发现并修复 5 个真 Bug

| # | Bug | 根因 | 修复 |
|---|---|---|---|
| 1 | **分镜生成偶发失败**（"模型未返回 JSON"或静默生成 0 镜项目） | AGNES 偶发 59s 空响应 / thinking 截断致 shots 空，重试只查 `{}` 不查 shots | 重试 3 次，校验「解析成功且 **shots 非空**」 |
| 2 | **③资产场景列表空 / 场景图生成崩** | 模型偶发输出 scenes 为 dict（`{SC01:{...}}`），前端按数组渲染 | load_spec 统一 **dict→list 规范化**（scenes+props，补 key/name） |
| 3 | **旧项目需求卡残留进新项目** | newProject 未显式清空 reqCard，旧值被 confirmReq 保存进新项目 meta | newProject 显式清空 reqCard/outlineData/visualStyle |
| 4 | **英文提示词中英混杂** | 情绪字段中文（如"疲惫孤寂"）直接拼进语音指令段 | 后端 `_emotion_en` + 前端 `emotionEn` 翻译表（40+ 词，未匹配中文→expressive） |
| 5 | **测试档合成成片却是 1080×1920** | 合成分辨率固定用 storyboard.resolution（模型输出 1080p），无视测试档 | assemble.py 加 `--resolution`，合成跟随 `_video_size()`（test 合 480×854） |

## 顺带修复
- **prompt_library.json 的 storyboard.prompt 是 08-10 旧快照（缺 scene_type 字段）**——这正是此前"分镜源头 scene_type 覆盖全漏"的根因（端点走库文件而非代码常量）。已同步最新 STORYBOARD_SYS（5450 字符含 scene_type）。

## 清理
- 7 个测试项目已删除，注册表只保留「晚安拿铁」（012147）成果项目
- QA 脚本已清，服务健康（studio 200）

## 结论
横屏/竖屏 × 测试/正式四档参数全部真实跑通；参数编辑→最终提示词→重渲全链路生效；批量、幂等、失败反馈、合成均正常。测试档全流程（含合成）产出 1.9MB 480p 成片，迭代速度大幅提升。
