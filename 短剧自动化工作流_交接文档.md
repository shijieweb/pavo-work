# 短剧自动化工作流 · 交接文档
> 整理：阿编 🎬 ｜ 日期：2026-08-09 ｜ 用途：新会话接手唯一依据
> 老板（南京）的数字创作副驾。本流水线目标 = 可发布/可变现的 AI 短剧（商用级），非玩具。

---

## 0. 一句话定位
全自动 AI 短剧创作工作流，基于**全云 API**（AGNES 生成 + MiniMax 配音 + 开源 ffmpeg 编排），当前主战场是 `short_drama_workflow/html_prototype/` 下的 **5 步流水线工作台**（`studio.html` + `server.py`）。老板不愿本地部署。

## 1. 当前架构（必读）
- **端口**：`8777`=studio 工作台(`server.py`)；`8787`=agnes_proxy 统一门户(`hub.html` 反代 8777/8788)；`8788`=共享任务看板。老板只能访问 8787。
- **5 步模块 ID**（前端 `activeModule` 值，不是中文）：
  - ① `story` 创意·需求卡　② `image` 资产设计（角色/场景/道具三 tab）　③ `video` 分镜校准·关键帧　④ `shots` 分镜视频　⑤ `advanced` 合并成片
- **数据契约**：`projects/<pid>/storyboard.json` 是浏览器与后端共用的唯一数据源；资产 URL 写回它。
- **模式**：`REAL=1` 真实调 AGNES/MiniMax（花额度）；默认 dry-run。

## 2. 本轮（2026-08-08~09）修复的 3 个 Bug —— 已验证在 `server.py`/`studio.html` 就位
| # | 现象 | 根因 | 修复位置 |
|---|---|---|---|
| **Bug A** | 编写分镜后数据不展示到②资产 | `generate_storyboard` 落盘但没载入全局 `SPEC`；`/api/spec` 遇 `project==ACTIVE` 短路返回旧数据 | `server.py` `/api/generate/storyboard` 路由加 `load_spec(ACTIVE)`；`/api/spec` 去掉短路（约 L2811/L2817） |
| **Bug B** | 无"新建初始化项目"入口 | 后端无建空白项目接口、前端无按钮 | `server.py` 新增 `POST /api/project/new`；`studio.html` 加「＋新建项目」按钮 + `newProject()`（跳到 `story` 模块，约 L46/L1109） |
| **Bug C** | ②批量出图时 `loading` 永久挂 → ③关键帧按钮一直 `disabled` | 图片生成函数串行裸调 AGNES **无超时兜底**（视频早有 `_gen_video_with_timeout`，图片漏了），某张图一卡整批永不返回 | `generate_references_real` / `_gen_image_assets_real` 加**单张 300s 硬超时**（ThreadPoolExecutor 包 `generate_image` + `ex.shutdown(wait=False)`，约 L1045/L1074/L1094/L1119） |

## 3. 已完成的清理（本会话做掉的）
- 删 4 个测试项目 `ep_0808_184139`(第一天) / `ep_0808_193952`(空白桩) / `ep_0808_194207`(小满的反击·完整生成) / `ep_qa_golden`(QA夹具)；`projects.json` 重置为 `[]`；删 `.active_project`。
- 删测试工具 `qa_*.py` / `qa_3round_result.json` / `qa_3round_run.log` / `qa_shots/`。
- **产物数据全清**：根目录 `office_drama/` `wenwan_meiren/` `zhengri_fengbao/` `zhuanzheng_dangtian/` `output/` `exports/`；`html_prototype/_backup_20260809_projects/` 快照；`server_run.log` 等散落 log。
- **会话数据清**：删全部 179 条历史任务；删 13 个日常记忆日志（`2026-07-07`~`2026-08-09`）。
- **保留**：`server.py` `studio.html` `agnes_client.py` `agnes_proxy.py` `hub.html` + 本目录 `.workbuddy/memory/MEMORY.md`（项目参考卡，长期保留）。

## 4. 新会话如何启动（干净起步）
```bash
cd C:/Users/67972/WorkBuddy/workbuddy/short_drama_workflow/html_prototype
REAL=1 "C:/Users/67972/.workbuddy/binaries/python/versions/3.13.12/python.exe" server.py
```
浏览器开 `http://127.0.0.1:8777/` → 点「＋新建项目」即从①重头开始。当前 `projects.json=[]`，下拉为空属正常。

## 5. 未竟事项（原 pending / in_progress 任务精炼，新会话按需重建）
> 这些是原任务列表里还没收口的工作。本会话已把 Bug A/B/C 修掉（原 #175/#176/#177），其余如下：

**交互/模块打磨**
- ① 解耦 + 需求卡片确认区（#147）　② 资产三 tab 完善（#149）　③ 接收分镜编辑工作台（#148）
- ④ 满意门禁 + ⑤ 导出成片（#150 / #158 导出下载按钮 / #159 放行校验 / #160 返工弹窗 / #161 合成清单）
- ② 素材库图片预览 404 链路（#126）　UI 样式/数据展示/手机适配全面检查（#125）　PAVO 对标（#127）

**能力扩展**
- 深度人脸模型 insightface/yunet 接入（#89）　二期 Agent 对话式入口（#109）
- 批量队列调度器 cron（#75）　整集质量调优（低分镜 prompt，#76）
- 返工 Gap D + 满意门禁状态机验证（#166）　分镜数据补全 gen_strategy/last_frame_prompt（#61）

**验证类**
- 三轮自测 + QA 扩展（#140 / #156 / #162 / #173）　①重渲后自动重建 final 闭环（#74）
- 注：3 轮**真实** e2e 未在本会话跑完汇总（真实视频额度/耗时），但 ①→② 全链路含 Bug A/B 已多轮实测 PASS；Bug C 为代码层超时兜底已就位。

## 6. 已知约束 / 坑（踩过的，别重踩）
- **视频额度**：每日 ~500s，真实出视频慢（每镜 5–20min）。3 轮完整真实出片预计数小时且可能触额度上限。
- **AGNES 网络**：`.com`(Cloudflare) 直连被出口防火墙 TLS 重置；须用 `.cn`(36.153.95.129) 直连。本地出口代理 `127.0.0.1:10808`（Clash/V2Ray）未起时 REAL 全断。
- **改 server.py 必须干净重启**：杀全部 `python server.py` 进程（PowerShell `Get-CimInstance Win32_Process` 按 CommandLine like '*server.py*'），确认 8777 端口释放再起，否则旧进程继续服务→"修复没生效"假象。
- **删项目须同步**：改 `projects/projects.json` 注册表 + 清 `.active_project`，否则 UI 下拉仍显示、点了 404。
- **Vue3 实例读取**：`document.querySelector('#app').__vue_app__._container._vnode.component.proxy`（不是 `#app.__vueParentComponent`）。
- **Playwright 驱动真实按钮**：stepper 切模块用 `button:has-text("资产设计")`；② 三批量按钮按 tab(role/scene/prop) 分栏，须先切 tab 再点；角色图字段是 `c.img`（非 `img_url`）；`loading` 状态是按钮 `:disabled` 的判据。
- **后台长任务铁律**：任何预期 >30s 的操作必须前台跑 + 等出结果第一时间汇报；长任务须每分钟报平安，不得静默。

## 7. 关键文件索引
| 文件 | 作用 |
|---|---|
| `short_drama_workflow/html_prototype/server.py` | 后端：所有路由 + 生成/质检/诊断逻辑 |
| `short_drama_workflow/html_prototype/studio.html` | Vue3+Tailwind 工作台（5 步流水线 UI） |
| `~/.workbuddy/skills/agnes-ai/scripts/agnes_client.py` | AGNES 网关封装（文/图/视频/诊断） |
| `agnes_proxy.py` + `hub.html` | 8787 统一门户（反代 8777/8788） |
| `.workbuddy/memory/MEMORY.md` | 项目参考卡（长期保留，含端口/密钥/铁律） |

---
*本文件替代全部历史会话日志与任务列表。新会话读完本文件即可接手，无需翻旧记录。*
