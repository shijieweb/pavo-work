# 短剧自动化工作台 · 与 Pavo「一句话成片」差距分析报告

> 生成时间：2026-08-07 ｜ 对标对象：Pavo 官方七步法 ｜ 当前版本：5 步生产管线（经 8787 门户访问）
> 结论先行：我们管线覆盖 Pavo 约 **70–80%**，缺失的是「需求卡片 + 场景/道具 + 导出」三块**加法式**能力，不破坏现有 5 步结构；同时在转场精细度 / 质检 / 透明度上已反超。

---

## 一、Pavo「一句话成片」七步法（对标基准）

| # | 步骤 | 关键能力 |
|---|---|---|
| 1 | 🧠 创意输入与解析 | 自然语言输入 → 语义解析 |
| 2 | 📋 制作需求卡片 | 自动生成结构化卡片：标题/梗概/时长/画幅(9:16)/拆分镜模式/视觉风格 —— **首次全局校准机会** |
| 3 | 🎨 角色/场景/道具设计 | 自动出图；自然语言定向修改迭代 |
| 4 | 🎬 分镜拆解与校准 | 拆 ~5 镜；人工校准；**锁定关键帧锚点**保连贯 |
| 5 | 🔄 镜头级返工 | 预览发现问题镜 → 单镜提修改要求，**不重生成全部** |
| 6 | 🎞️ 批量渲染与合成 | 并行渲染；自动加 BGM / 转场 / 统一画质 / 音画同步 |
| 7 | 💾 导出成片 | 导出 / 下载完整视频 |

**用户视角 5 阶段**：启动输入（选「剧情短片」/「Agent」）→ 确认调整（需求卡 + 设计图文字改）→ 微调返工（点镜改 / 弹窗描述问题）→ 合成导出（标记满意 → 合成 → 进度 → 下载）。

---

## 二、我们当前 5 步管线

| # | 步骤 | 能力 |
|---|---|---|
| ① | 创意·分镜 | 粘小说 / AI 生成 → 文字分镜表 + 素材库 + 单镜精控；Agent 折叠 |
| ② | 人物资产 | 角色图批量 / 单张生成；改 `img_prompt` 重生成 |
| ③ | 关键帧资产 | 每镜首 + 尾帧图；改尾帧提示词重生成 |
| ④ | 分镜视频 | 每镜首/尾帧缩略图 + 入/出转场 + 保存转场 + 生成视频 + 保存并重渲 |
| ⑤ | 合并成片 | 选转场 + 合并 + 健康卡 + 分发占位 + 高级工具 |

---

## 三、逐步骤覆盖对比

| Pavo 步 | 我们对应 | 覆盖 | 差距说明 |
|---|---|---|---|
| 1 创意解析 | ① 创作入口 | 🟢 全 | 我们缺「语义解析反馈」展示，但功能等价 |
| 2 需求卡片 | —— | 🔴 缺 | **无全局参数校准卡**（标题/时长/画幅/风格/拆镜模式） |
| 3 角色/场景/道具 | ② 仅角色 | 🟡 部分 | 缺 **场景图 / 道具图** 设计步骤 |
| 4 分镜校准 | ①分镜表 + ③关键帧 | 🟡 部分 | 有首/尾帧但无「**锁定锚点**」显式动作 |
| 5 镜头返工 | ④保存并重渲 / ③重生成 | 🟡 部分 | 返工 = 提示词重生成，**非「描述问题」自然语言弹窗** |
| 6 渲染合成 | ④ + ⑤ | 🟡 部分 | 分镜级转场更强；但缺「**自动 BGM / 统一画质**」显式表达 |
| 7 导出 | ⑤ 预览 + 占位 | 🔴 缺 | 无 **导出 / 下载** 按钮 |

---

## 四、差距清单（按严重度）

- **Gap A（高）· 缺制作需求卡片**
  Pavo 在创意后先出一张全局参数卡让用户校准，我们直接进分镜表。结果：画幅 / 时长 / 风格 / 拆镜数这些全局设定没有「确认闸门」，只能靠改 spec 或吃默认。
  → 建议：① 内新增「需求卡片」确认区（卡片式展示 + 可编辑），确认后才生成分镜。

- **Gap B（高）· 缺场景 / 道具资产**
  Pavo 三步资产（角色 / 场景 / 道具），我们仅角色。场景一致性靠关键帧隐式承载，但用户无法像改角色一样改场景 / 道具图。
  → 建议：② 扩展为「资产设计」（角色 + 场景 + 道具三 tab），或在 ③ 前加一个小步。

- **Gap C（中）· 缺导出 / 下载**
  ⑤ 只有 `finalUrl` 预览 + 分发占位，无「下载 / 导出」实体按钮。
  → 建议：⑤ 加「⬇ 导出成片」按钮（触发下载 / 落盘）。

- **Gap D（中）· 返工方式差异**
  Pavo 是「点击问题镜 → 弹窗描述问题 → 系统改」；我们是「改提示词 → 重生成」。功能可达但交互不如 Pavo 流畅，对非专业用户门槛高。
  → 可选：④ 加「描述问题」弹窗，后端把自然语言转成提示词。

- **Gap E（中）· 缺「标记满意」质量门禁**
  Pavo 所有镜标记满意才允许合成；我们 ④ 有 status 但无门禁，可直接跳 ⑤。
  → 建议：④ → ⑤ 加「全部镜已生成 / 满意」校验。

- **Gap F（低）· 合成阶段能力未显式表达**
  BGM / 统一画质 / 音画同步在 ⑤ 有参数但无「自动套用」反馈。我们的原生音画同步（agnes-v2.0）其实已强，但 UI 没讲清楚。
  → 建议：⑤ 加合成清单（已加 BGM / 转场 / 画质统一 / 音画同步）。

---

## 五、我们优于 Pavo 的点（务必保留）

1. **分镜级入 / 出转场**：Pavo 转场在合成阶段全局统一；我们每镜可独立设入 / 出转场，控制更细。
2. **健康卡 + 质检诊断**：`quality_check` + AGNES 4 维诊断，Pavo 未显式提供。
3. **关键帧资产显式可控**：③ 把首 / 尾帧当独立资产展示 + 重生成，比 Pavo 抽象「锁锚点」更直观可改。
4. **流程透明**：5 步全可见，用户始终知道在哪、下一步干嘛；Pavo 更「黑盒自动化」。

---

## 六、优化路线图（建议优先级）

- **P0（结构补强，先做）**：Gap A 需求卡片、Gap B 场景 / 道具资产。
- **P1（体验闭环）**：Gap C 导出按钮、Gap E 满意门禁。
- **P2（交互升级，可选）**：Gap D 自然语言返工弹窗、Gap F 合成清单可视化。

> 一句话：Pavo 是「黑盒七步」，我们是「透明五步 + 精细转场 + 质检」。补上需求卡片 / 场景道具 / 导出三块，体验即可追平并局部超越；无需推倒重来。

---

## 附：与上一轮「③ 改名」的衔接

上一轮已将 ③ 由「分镜资产」改名为「**关键帧资产**」，正是为了和 ① 的文字分镜表（计划层）区分开——本报告确认该改名方向正确，③ 对应 Pavo 第 4 步「分镜校准」的视觉素材层。
