# 专家模式通道验证报告 · 2026-08-13

> 主理人（阿编）接手新窗口后，按交接文档 §3 执行「第一步：重建专家 + 极小探测验证通道」。

## 验证结论：专家模式通道 ✅ 全通

| 验证层 | 验证方式 | 结果 |
|---|---|---|
| ① 连接层（专家中心连专家） | `search_plugins`(type=expert) 搜候选 → `suggest_plugin_install` 发连接卡片 → 老板点确认框 → 专家被选中 | ✅ `SoftwareCompany`（软件开发团队）已连上并选中 |
| ② 派活层（主理人→子成员→回传） | `TeamCreate software-shortdrama` → 派 `software-qa-engineer` 读 `current_state.md` 第一行 | ✅ 子成员读真实文件、回传真实首行 |

### 证据（坑3 防"空返回"：独立比对）
- QA 工程师逐字回传：`# dev-work · 开发/测试协作状态台`
- 主理人开局独立 Read 该文件第一行：一致 ✅
- 结论：子成员读了**真实文件**、回了**真实内容**，派活链路闭环，非编造。

## 七坑对照（接手时逐条自检，无重蹈）
- 坑1 嘴上执行实为不调：本轮每一步（读文档/搜专家/发卡片/建团队/派活/复核）均对应真实工具调用。
- 坑2/5 派活卡死先疑"确认框没点"：连接卡片发出后，第一时间提示老板点确认框；老板点了即通，未误判"工具坏"。
- 坑3 不验证产出：QA 完成通知只给"成功"摘要，无原文 → 主动 SendMessage 要逐字回传比对，确认非空返回。
- 坑4 两套机制叙事：全程统一"专家模式 = subagent 机制 = 同一回事"，未割裂。
- 坑6 主理人越界写码：仅调度 + 收证据，未亲自写项目代码（探测由 QA 子成员执行）。
- 坑7 节奏：探测为短任务，未刷屏；长任务才需每分钟报平安（本次不适用）。

## 当前状态
- 专家「软件开发团队」已就位（专家团形态：产品经理/架构师/工程师/QA）。
- 团队 `software-shortdrama` 已建（lead = team-lead），含 idle 的 QA 工程师，待用。
- 下一步候选（来自交接 §5 backlog）：
  1. **l1_smoke 固化进回归套件**（推荐先做：价值高、零风险、巩固免费KEY真测纪律）
  2. **P0-2 图视冲突预检**（推进生成质量，依赖 P0-1 YAML）
  3. **P0-4 跨 seed 一致性**（后续）

> 待老板从 backlog 选定下一个任务后，走 GATE0 自检 → 四文档分离 → 五态状态机 → 三道人闸正式派活。
