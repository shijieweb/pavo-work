# T-20260813-04 验收（acceptance）

## AC 勾表
| AC | 描述 | 结果 | 证据 |
|---|---|---|---|
| AC-1 | 无 token `GET /studio` → 200 | ✅ | [test.md#一] |
| AC-2 | 无 token `GET /api/projects` → 200 | ✅ | [test.md#一] |
| AC-3 | 无 token `PUT /api/spec` → 非 401 | ✅ | [test.md#一] |
| AC-4 | 8787 由 schtasks 新 PID 持有 | ✅ | [test.md#二] |
| AC-5 | 内部 8777 自愈 → 200 | ✅ | [test.md#三] |

## 主理人把关结论
- **放行决定**：✅ 放行（完成）
- **亲自复验**：主会话实测全部 AC（真实 HTTP 请求 + 进程/PID 核验），非子角色文本
- **闭环**：老板决策 → 开发派活（静默空返回）→ 主理人接管执行 → 主会话实证 → 验收
- **本次发现**：
  1. [已处理] 开发子 agent 第 2 次静默空返回（无 commit/无改动）→ 主理人按 SOP §3.6 接管
  2. [已接受·残余风险] 公网隧道无鉴权暴露（老板明确：仅本地单人使用，接受）
  3. [已固化] 计划任务 `AgnesPortal` 托管 8787，agent 掉线服务仍存活
