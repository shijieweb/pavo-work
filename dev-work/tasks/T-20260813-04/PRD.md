# T-20260813-04 取消鉴权 + 8787 系统自启（PRD）

> 关联：T-20260813-03（令牌闸）→ 本任务按老板决策回退；老板明确"仅本地单人使用，默认取消鉴权"。

## 需求
1. **回退令牌闸**：删除 agnes_proxy.py 的 PORTAL_TOKEN 鉴权（commit `dec5ce9`，34 行删除），恢复全开放。原 D4 决策前提已由老板明确：本地单人使用。
2. **8787 系统自启**：注册 Windows 计划任务 `AgnesPortal`（onlogon 登录自启），代理进程独立于 agent 会话存活。
3. **不做 /unlock cookie**（老板：3 一样，取消）。

## 验收标准（AC）
| # | 验收 | 状态 |
|---|---|---|
| AC-1 | 无 token `GET /studio` → 200 | ✅ |
| AC-2 | 无 token `GET /api/projects` → 200 | ✅ |
| AC-3 | 无 token `PUT /api/spec` → 非 401（业务层 400，证明无鉴权拦截） | ✅ |
| AC-4 | 8787 由 schtasks 新 PID 持有（非 agent 会话进程） | ✅ |
| AC-5 | 内部 8777 自愈检查 → 200 | ✅ |

## 边界
- 不回退 T-20260813-03 的任务文档/审计（保留决策记录）。
- 公网隧道暴露为**已接受的残余风险**（老板决策：仅本地单人）。
- 子 agent（开发）再次静默空返回 → 主理人按 SOP §3.6 主会话接管执行。
