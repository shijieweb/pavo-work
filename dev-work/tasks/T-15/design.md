# design · T-15（研发填写，推「待验证」即停）

> 开发按此填：实现方式 + git diff（若改仓库）/ 生成器逻辑 + 自测证据 + 面板运行态截图。
> 无输出=未测=不通过。

## 实现说明
（开发填写：build_training_panel.py 如何读 CSV/扫描 PNG/inline 数据；HTML 结构）

## 自测证据
（开发填写：跑生成器命令 + 输出 HTML 路径 + 确认 54 图全在的数据校验，如「console 输出 54 条记录」）

## 视觉样片
（开发填写：面板运行态截图 / 或描述网格+开关+筛选效果）
