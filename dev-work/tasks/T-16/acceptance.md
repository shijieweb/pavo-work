# acceptance · T-16（主理人把关填写，对照表逐条勾，附证据）

> 阿编终验：对照每条 AC 勾选 + 证据；全部 PASS 才标「完成」。

## 主理人读盘核产（§4.3，独立于研发自述，直接解析产物）
- `training_panel.html` = 163,729 B（自包含，远低于 5MB 上限）
- `<img` 总数 = 56（54 缩略图 + 2 角色参考图）；tag 级 `data-role="thumb"` = 54（第 55 处是 JS 选择器字符串 `closest('[data-role="thumb"]')`，已确认为自检拦下的假阳性，非多图）
- 54 个唯一 `wXX_Y.png` 全部出现，**0 缺失**
- `data:image/*;base64` 出现 **0 次** → 未把 194MB 图内嵌（铁律保住）
- 中文锚点「同一个齐肩黑发」出现 **54 次** → 中文已真内联；`prompts_zh.csv` 完整 54 行
- `data-writing` 去重 = 27（1–27 连续），分组成立
- `id="lightbox"` / `normalizeGroup` / `双图优` / `主图` / `备选` / `弃` 字符串存在
- git `88d24aa` 已提交 `design.md`

## AC 对照表
| AC | 证据 | 结果 |
|---|---|---|
| AC-1 点击放大 | lightbox DOM 齐备（backdrop 点空白关 / ESC 关 / 中英双栏）；QA 行为侧 14 断言全过 | ✅ PASS |
| AC-2 中英对照 | `prompt-zh`=54、`prompt-en`=54 成对；主理人抽查 + QA 全量强校验 54/54 全文命中 0 截断；写法号 24 源数据本身中英混排（忠实透传，非 T-16 引入） | ✅ PASS |
| AC-3 组+图三态 | 组三态×27 + 图三态（主图/备选/弃）×54；QA 结构核验确认 | ✅ PASS |
| AC-4 双图处理方案 | ①门槛：全弃组点采纳被拒、保持 pending ②双图优：两张都好→自动 adopt+强制恰好1主图 ③导出 CSV 27 行带主图文件名、JSON「采纳无主图」=0；1200 次 fuzz 不变量零违例；面板内「两张都可以怎么办」规则块存在 | ✅ PASS |
| AC-5 统计新增 | 6 格含 写法号采纳数 / 双图优数，保留 T-15 四项 | ✅ PASS |
| AC-6 持久化+导出 | localStorage 存组态+图态；导出 JSON(groups27/images54) + CSV(1行/组·BOM·27组全带主图)；QA 实跑确认 | ✅ PASS |
| AC-7 铁律不变 | 56 img / 54 唯一 / 0 base64 / 0 截断 / 27 组，均成立，未破坏 T-15 | ✅ PASS |
| AC-8 阶段+参考图 | 阶段说明 + 角色参考图区（2 图磁盘均在）保留 | ✅ PASS |

## 阿编把关结论
**放行决定：✅ 放行（完成）。** AC-1~8 全部 PASS；老板铁律「54 图一张不漏」经主理人独立读盘 + QA 独立验收（188 断言 0 FAIL）双重交叉验证满足（54 唯一图 0 缺失、0 截断、0 base64 内嵌）。「两张都可以」处理方案（AC-4）经 1200 次 fuzz + 脏数据自愈测试确证规则成立、导出无歧义。

闭环流程跑通：engineer-t16 构建（推待验证）→ qa-t16 独立验收 188 断言 0 FAIL（推已验证）→ 主理人读盘核产 + 填 acceptance → 放行完成。研发/测试分离铁律已守。

## 上线须知（转老板）
- 面板路径：`C:\Users\67972\projects\short-drama-training\training_panel.html`（双击 file:// 打开，离线可见全部 54 图 + 中文 + 采纳开关 + 点击放大）。
- 三项新能力：①点缩略图弹大图（点空白/ESC 关）②每张卡中英对照（中文在上绿边、英文在下蓝边）③采纳上移到写法号层级 + 每张图主图/备选/弃；两张都好自动「采纳·双图优」且强制恰好 1 张主图。
- 图片仍走本地相对路径 → **面板必须留在训练项目根**；采纳存 localStorage（换浏览器/清缓存丢）→ 审完点导出存 `04_采纳区/`。
- 真机渲染 + lightbox 观感需老板开浏览器确认一眼（无头环境无法解码 PNG，已结构兜底）。
- 中文翻译为一次性 `prompts_zh.csv`（阿编译），生成器只读合并；写法号 24 英文 prompt 源数据本就中英混排，面板忠实透传（清洗需另授权任务）。
