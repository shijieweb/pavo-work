# test · <T-ID> <任务名>

> 模板来源：`dev-work/templates/TEMPLATE_TEST.md`。**测试填写**，推「已验证」时一并交付。
> 铁律：独立验证亲自跑（非研发自报）；无 P0/P1；每条结论附证据。测试**不修 bug**。

---

## 一、测试用例 + 覆盖矩阵

| 用例ID | 对应 AC | 输入/动作 | 预期 | 实际 | 结果 | 证据 |
|---|---|---|---|---|---|---|
| TC-1 | AC-1.1 | [输入] → [动作] | [预期] | [实际] | PASS/FAIL | [命令/截图] |
| TC-2 | AC-1.2 | ... | ... | ... | ... | ... |
| TC-3 | AC-1.3 | ... | ... | ... | ... | ... |

> 覆盖矩阵必须 100% 覆盖 PRD 的每条 AC。未覆盖的 AC 要写明原因。

---

## 二、L1 真·管线冒烟（触及生成逻辑必做，用免费KEY）

- **是否触发 L1**：是 / 否（否需说明本任务不触及生成逻辑）
- 免费KEY：`AGNES_TEST_API_KEY`（无限额度，仅排队，不占 VIP）
- 真实镜头：模板 [X] + 真实 assets [路径] → 调 `gen_video`
- 命令 + 输出：
```
<真实调用命令与 AGNES 返回（job id / 视频 url）>
```
- 断言：payload（keyframes/data_uri/negative/seed）真传到 AGNES、返回可拼 → [PASS/FAIL]

---

## 三、重跑研发回归（不盲信研发输出）

```
<自己重跑回归脚本的真实命令与输出>
```

---

## 四、缺陷清单（[BUG] 格式，仅报告不改）

`[BUG][S<严重度>|P<业务级>] 一句话现象 (AC-<编号>)`
最小字段：复现步骤 / 期望 / 实际 / 环境 / 严重度 / AC 锚点 / 截图或日志(必带)

- `[BUG][S?|P?] ... (AC-x.y)`
  - 复现：
  - 期望：
  - 实际：
  - 环境：
  - 截图/日志：

---

## 五、整体结论

- [ ] 建议阿编放行 / [ ] 退回开发修复（阻塞项：...）
- 覆盖矩阵：AC-1.1~x.y 全部 PASS / 存在 FAIL
- P0/P1：无 / 有（列出）
- 证据存档：本文件 + [截图/日志路径]
