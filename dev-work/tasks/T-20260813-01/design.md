# design · T-20260813-01 l1_smoke 固化进回归套件

> 模板来源：`dev-work/templates/TEMPLATE_DESIGN.md`。**开发填写**，推「待验证」时一并交付。
> 铁律：无输出 = 未测 = 不通过。以下每节都要填真实内容，禁止空话。

---

## 一、实现方案

- （待开发填写：思路 / 关键改动点 / 为什么这么做）
- 与现有逻辑的兼容处理（如不改变 `main()` 调用约定）

---

## 二、接口契约（如有函数/模块改动）

| 项 | 说明 |
|---|---|
| 函数签名 | `def xxx(...)` |
| 输入字段 | [字段名: 类型: 含义] |
| 输出字段 | [字段名: 类型: 含义，含下游消费方需要的] |
| 下游消费方 | [谁读这个输出，字段必须对齐] |

---

## 三、自测证据（铁律：无输出 = 未测 = 不通过）

### 3.1 改动文件清单（git diff）
```
<粘贴 git diff --stat 输出>
```

### 3.2 本机跑测试的真实命令 + stdout
```
<粘贴真实命令与输出，如 "200 passed / 0 failed">
```
> 禁止调用 `gen_video` / `main()` 烧额度（L0 阶段）。真测走 L1 免费KEY（见 `运行手册.md` §8）。

### 3.3 关键运行截图 / 日志
- [截图路径或日志摘录，能打开复核]

### 3.4 可真跑的启动 / 调用命令
```
<别人照抄就能复现的命令>
```

---

## 四、提测说明（测试怎么接）

- 测试入口：[命令 / 函数 / 参数]
- 待测范围：[哪些 AC 对应哪些变体/路径]
- 已知限制：[本次未覆盖的，标注非阻塞]

---

## 五、文档回写

- [ ] `design.md` 已填（本文件）
- [ ] 任务卡 AC 进度已更新到 `current_state.md`
- [ ] 其他四文档更新：[...]
