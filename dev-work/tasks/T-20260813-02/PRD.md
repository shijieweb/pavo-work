# 任务卡 T-20260813-02 · 8787 统一网关：路由收敛为 route_registry.json 单一事实源

- 需求基线闸：老板已签 ☑（2026-08-13 02:08 拍板：**只做入口层，其他不动**）
- 目标：把 8787 统一网关的路由配置收敛为 `route_registry.json`；**加服务 = 注册表加一行，对外永远只有 8787**。
- 产出路径：新增 `route_registry.json`（或 `config/routes.json`）+ 改 `agnes_proxy.py`（路由来源改为读注册表；**无注册表时回退现有白名单**，保证兼容）。

## 验收标准（AC 锚点）

- [ ] AC-1.1 注册表含 `/studio→8777`、`/board→8788(board-token + rewrite)` 两条真实路由，加载即生效
- [ ] AC-1.2 **前缀唯一校验**：重复前缀启动即报错（防两服务抢 `/api/`）
- [ ] AC-1.3 `do_PUT`/`do_DELETE` 对注册表内**所有前缀**生效（不再只 `/api/*`）
- [ ] AC-1.4 **路由清单 diff 回归脚本**：每前缀 curl 断言 200/404/转发目标（零 AGNES 额度）
- [ ] AC-1.5 示例：新增假想服务条目（`127.0.0.1:8779`），验证"注册表加一行即通"，对外仍仅 8787

## 证据要求

- 开发：git diff + 回归脚本输出（每前缀 200/404/转发目标）+ 启动日志
- 测试：独立实跑回归 + 老板实机 8787 访问验证（`/studio` `/board`）

## 边界（老板定）

- 只动入口层（`agnes_proxy.py` 路由来源 + 注册表）；**生成链 / 云 API / 多角色框架一律不碰**
- 不新增对外端口；新服务一律 `127.0.0.1`
- 不引入新依赖；不重构反代核心转发逻辑（只换路由来源）

## 备注（旧窗口 02:11 留言）

- l1_smoke 收尾后自取本卡；派活前走 GATE0 + 闸1；动 `agnes_proxy.py` 前 `git commit before:`
- 改完必跑：路由回归 + **干净重启 8787**（杀残留再起、核实新 PID 绑定）
