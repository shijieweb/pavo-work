# 分析师留言 #1 · l1_smoke 返工提醒 + 对外入口体检 + T-02 规格卡就绪

- **投递时间**：2026-08-13 02:16 ｜ 分析师
- **优先级**：中（不阻塞你们当前返工，看完按需取用）

## 1. ⚠️ l1_smoke 返工提醒（重点）
- `l1_smoke.run.log` 目前停在 **01:54 失败版**（`FileNotFoundError: html_prototype\server.py`）。
- 真跑通过后**务必覆盖/追加成功日志**——否则验收者第一眼看到旧失败日志会误判最新状态（分析师就差点看错）。
- 验收锚点：① 入口 assert `mode=="test"` 守卫生效（防误烧 VIP）② data_uri 真覆盖 ③ 零 VIP 额度 ④ 真出片证据（last_url + curl 200 + git commit）。

## 2. ✅ 对外入口体检（2026-08-13 02:13 实测）
- **http://agnes.owen1.de5.net/** = 8787 统一网关门面，老板对外唯一入口。
- 实测全通：根=200(0.4s, hub 控制台)、`/board`=200、`/studio`=200、`/vendor/`=200。
- ⚠️ **意见**：公网明文 HTTP 且 /studio 无登录态直接 200（含 projects 数据 + PUT /api/spec 改文件能力）。D4 鉴权"不做"是本地网络旧决策，对外公开后建议重估——至少 studio 加访问口令、确认 board token 公网生效。此意见不要求现在做，排期讨论即可。

## 3. 📄 T-20260813-02 规格卡已备好（自取）
- **`dev-work/tasks/T-20260813-02/PRD.md`**：8787 统一网关路由收敛为 route_registry.json（加服务=注册表加一行，对外仅 8787）。
- 老板已拍板方向：只做入口层，其他不动。l1_smoke 收尾后自行开卡（GATE0 + 闸1；动 agnes_proxy.py 前 `git commit before:`）。

## 4. 边界声明
- 分析师只写文档/分析/意见，**不碰代码**；交接文档已封版，信息一律走本箱。

---
*看完请把本文件移入 `archive/`，并在 `LATEST.md` 标记已读。有疑问在本文件末尾追加一行即可。*
