# 分析师留言箱 · README（团队必读，一个文件全知道）

> 2026-08-13 02:26 ｜ 分析师（原主理人转岗）｜ 老板只转发本文件 → 下面全部看完即可，无需再看别的。

## ⚡ 一句话使用指南（双向）
- **你们**：看完本文件 → 把 `inbox/` 里对应留言移到 `archive/`（顺手在 `LATEST.md` 标已读）→ 有话说写进 `reply/`（带时间戳）+ 让老板带话一声。
- **我**：有新分析/意见 → 写进 `inbox/` + 更新 `LATEST.md` → 老板转发给你们；有空看 `reply/` 回信，读完移 `archive/`。

## 我是谁
你们团队的分析师（原主理人转岗）。职责边界：
- ✅ 出分析 / 提意见 / 风险预警 / 体检 / 备规格卡
- ✅ **可承接**：不紧急但需要时间 / 研究类的活（镜头实验设计、方法论验证、平台/竞品调研、模型对比、排障思路、备料）——写 `reply/` 找我
- ❌ 不写代码（代码由你们开发落地）/ 不碰生产 / 不接紧急救火（在途任务仍你们主理人-开发-测试闭环自跑）
- ❌ 不再改交接文档（已封版）

## 箱子结构
| 目录 | 方向 | 作用 |
|---|---|---|
| `inbox/` | 我 → 你们 | 未读留言（带时间戳 MD） |
| `reply/` | 你们 → 我 | 回信（带时间戳 MD），我看完移 archive/ |
| `archive/` | 双向 | 已读 / 已回复存档 |
| `LATEST.md` | 摘要 | 一句话留言 + 时间，滚动更新 |

## 📨 当前未读留言（全文内联，只看本文件也不漏）

> #1 / #2 / #3 已于 16:45 归档至 `archive/`（已处理，无需再看）。以下为**当前待处理**：#4 / #5 / #6 / #7 / #8
> 📌 #4~#7 已分别被 T-07（热加载）/ T-08（UI 融合）吸收，看完可一并归档。

### #1 l1_smoke 返工提醒 + 对外入口体检（02:16 投递）
1. ✅ **run.log 覆盖提醒（已解决 02:15）**：run.log 已更新为成功版（82.7s 真出片）——此条不用再处理。
2. ✅ **对外入口体检**（02:13 实测）：**http://agnes.owen1.de5.net/** = 8787 统一网关门面，根/`/board`/`/studio`/`/vendor/` 全 200。⚠️ 公网明文 HTTP 且 /studio 无登录态（含 projects + PUT /api/spec 改文件能力）——D4 鉴权"不做"是本地旧决策，对外公开后建议重估（至少 studio 加访问口令、确认 board token 公网生效）。排期讨论即可，不要求现在做。
3. 📄 **T-20260813-02 规格卡已备好**：`dev-work/tasks/T-20260813-02/PRD.md`（8787 路由收敛为 route_registry.json，加服务=注册表加一行，对外仅 8787；老板已拍板只做入口层其他不动）。l1_smoke 收尾后自取开卡（GATE0 + 闸1；动 agnes_proxy.py 前 `git commit before:`）。

### #2 分析师服务条款（02:24 投递）
- **一句话**：不紧急、但需要时间/研究类的活都可以找我——写 `reply/` 或让老板带话，我来接。
- **可帮**：研究类（镜头训练实验设计、prompt/首尾帧方法论验证、平台规则与竞品调研、模型选型对比）／分析类（架构评估、风险预警、日志/数据/生成结果分析、额度体检）／备料类（规格卡、方案对比、模板整理）／查错类（排障思路、证据核对——**思路我给，代码你们改**）。
- **边界**：不写代码 / 不碰生产 / 不接紧急救火；我管"慢活、深活、研究活"。

### #3 l1_smoke 返工闭环观察（02:28 投递 · 恭喜跑通）
- ✅ **真出片确认**（02:15 日志实查）：02:13:49 结束、**82.7s**、成片 URL `platform-outputs.agnes-ai.space/.../task_BOikAsJFPLqTuYO95uO2YUqL71MvyzXb.mp4`、**HTTP 200 / video/mp4**、全程 **mode=test 零 VIP**、`has_test_key=true`——AC 锚点（assert 守卫生效 / data_uri 真覆盖 / 零 VIP / 真出片）全达标迹象，返工闭环达成。
- ⚠️ **观察 1（架构小瑕疵，非阻塞）**：加载 `server.py` 触发其模块级 logging 初始化，与本机常驻 8777 争用 `logs/server.log` 产生非致命 PermissionError 噪声（被吞、不影响结果）。建议后续：加载 server 前禁用其 logging 或独立日志文件。
- ⚠️ **观察 2（证据核对）**：成片 URL 证据文件实际名为 **`l1_smoke.last_url.txt`**——测试验收时确认 test/acceptance 写的路径与真实文件一致，防 S4 证据路径坑重演。
- **下一步**：真出片 + 证据 + `git commit` 后推「待验证」→ 测试独立 L1 验收 → 主理人把关；T-02 收尾后自取。

### #4 注册表热加载（18:22 投递 · T-02 已完成，后置增强）
- **背景**：老板问"为什么每次都要重启 8787、不能热重载"。核实：route_registry.json 已落地（T-02 已完成 17:40：33 路由，`_load_route_registry()` 有前缀冲突校验+回退），但**仅启动读一次** → 改注册表仍要重启。
- **建议**：把注册表加载改为 **mtime 惰性重载**——照抄项目现有 `_BOARD_TOKEN_CACHE` 先例（`agnes_proxy.py:63-85`）：每请求 stat mtime，变化才重载。效果：**改注册表 = 零重启、秒级生效**。
- **边界**：前缀冲突校验保留（冲突 → 拒绝加载、沿用旧路由）；热加载只覆盖声明式路由变更；**代码逻辑变更仍走干净重启**（防脏双进程 S1P0）。
- **验收锚点**：改注册表加测试路由 → 不重启 → 5 秒内生效；冲突注册表 → 拒绝且旧路由可用。
- **挂靠**：T-02 已闭环 → 并入**「看板 v2 外部 API」任务**或新开小卡。老板 18:20 提议，方向明确。

### #5 UI 友好度研究报告（19:02 投递 · 三界面体检）
- **结论**：门户(hub)最优 ⭐⭐⭐；看板(board)功能全但 3 处硬伤 ⭐⭐；工作台(studio)专业但重 ⭐⭐。
- **看板 3 处 P0 硬伤**：①详情入口隐蔽（桌面要双击/手机要长按，无提示）→ 卡片加显式详情按钮或单击=详情；②无整体进度概览 → 顶部进度条（X 进行中·Y 待验证·Z 完成/总数）；③删除按钮过显眼 → 弱化为次级按钮。
- **P1**：状态筛选 / alert 改 toast（状态中文已由 T-07 覆盖）。
- **建议**：3 处 P0 都是 index.html 小改，**随 T-07 同批做**（一次干净重启）。

### #6 UI 深度综合分析（19:04 投递 · 使用 + 视觉两维度）
- **视觉 P0**：看板亮色 vs 门户暗色**主题割裂**——从门户进看板暗→亮断裂，建议加暗色或统一。
- **使用 P1**：保存成功无反馈（成功静默）；一键启动 30s 超时静默；看板无搜索/状态筛选；长按 vs 点击冲突。
- **视觉 P1**：优先级"中"灰色对比弱；**T-07 新态配色同批规划**（待验证=amber、已验证=purple）；门户卡片层级失衡（老板常用的工作台+看板应前置双列）。
- **排期**：T-07 同批 = #5 三处 P0 + 新态配色 +（可选）看板暗色；P1 开后续小卡。

### #7 与参考看板 kanban.html 深度对比（21:50 投递 · 借鉴清单）
- **定位**：参考=泳道式**进度可视化**（读场景·纯只读·静态 JSON）；我们=树状**任务管理**（读写·后端/权限/审计）。
- **值得学**：①统计条+列计数（T-07 进度概览条现成形态）②**暗色主题**（CSS 变量+🌙+localStorage，~30 行解"门户暗/看板亮割裂"P0）③toast ④泳道总览视图（留后续卡，需 deadline/milestones 字段）。
- **不学**：静态 JSON+DEMO、无编辑/权限/审计、自动刷新 60s 写死（我们 T-07 的"可配置+异步刷树"更优）。

### #8 综合收尾清单（22:08 投递 · 待闭环/可优化/风险）
- **好消息（22:08 实查）**：board 已中文 5 态（T-07 状态迁移落地）、#23/#26 已标完成、/ext 被远程指导实测、审计活跃。
- **待闭环**：①核产 T-07 剩余（校验/docs/热加载）②**远程指导留言不可见**（建议并入 T-08 加留言栏）③留言箱 #4~7 归档 ④#24/#25 backlog。
- **可优化**：每日审计对账定时化（audit_lint 每日跑）／board.db 每日备份／GBK 一行修复／/ext 留言用 curl 发送（docs 注明）。
- **风险**：公网无鉴权（已接受·登记）；子 agent 静默（§3.6 制度化）。

## ✅ 看完请做（三件小事）
1. 把 `inbox/` 里 **#4 / #5 / #6 / #7 / #8** 移到 `archive/`（#1~#3 已归档）。
2. 在 `LATEST.md` 把状态改成「✅ 已读」。
3. 有话说：`reply/` 写带时间戳留言 + 让老板带话一声，我来看。

---
*以后我每条新留言都会同步内联到本文件「当前未读」区 + 投 `inbox/` 存档。老板只转发 README，你们就不会漏。*
