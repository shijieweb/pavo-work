# 短剧自动化创作工作流 · 技术方案（商用级）

> 目标：依托 **AGNES-AI（老板已购 VIP，统一网关）** 完成视频与图片创作 + GitHub 开源编排，搭建从选题到分发的**全自动**剧情短剧生产线，达到国内平台（抖音 / 快手 / 视频号）商用上线标准。

---

## 一、已确认的核心决策

| 维度 | 决策 | 影响 |
|---|---|---|
| 内容形态 | AI 角色剧情剧 | 角色一致性是技术核心，靠 AGNES 图生视频 / 关键帧锚定 |
| 创作引擎 | **AGNES-AI（VIP）统一负责视频 + 图片** | 文本 / 图像 / 视频同一网关（OpenAI 兼容），规避多厂商锁定 |
| 部署策略 | 生成层全云 API | 视频 / 图像走 AGNES，配音走云 TTS，编排与剪辑用开源脚本 |
| 目标平台 | 国内（抖音 / 快手 / 视频号） | 竖屏 9:16，单集 1–3 分钟，需过审 + AI 标识 |

---

## 二、技术栈选型（AGNES 为中心）

| 环节 | 选型 | 备注 |
|---|---|---|
| 编排底座 | Python 脚本（AGNES 调用封装） | 轻量、可控，后续可接 ComfyUI / ZJT 参考 |
| 选题 / 剧本 | AGNES `agnes-2.0-flash`（同网关） | 多角色 Agent 编剧，OpenAI 兼容，256K 上下文 |
| 角色 / 场景图 | AGNES `agnes-image-2.1-flash` | 文生图 / 图生图，固定 seed 生成一致性参考图 |
| 视频生成 | AGNES `agnes-video-v2.0`（VIP） | 文生视频 / 图生视频 / 多图视频 / 关键帧动画，异步 API |
| 角色一致性 | 参考图 + 图生视频 + 关键帧绑定 | AGNES 视觉一致性原生支持 |
| 配音 TTS | **Edge TTS（微软免费接口，零 key）**为主，Pavo 作半自动精修备选；GPT-SoVITS / Fish Audio / 火山 / 腾讯云 作后备（暂未接入） | AGNES 网关无 TTS API；Edge 为非正式接口仅限开发验证(商用授权灰色)，量产商用引擎待定 |
| 剪辑合成 | ffmpeg + moviepy | 本地 / 云 ECS，批量出片、加字幕 |
| 审核合规 | AI 标识水印 + 平台规则预检 | 国内平台强制 |
| 自动分发 | 平台开放 API / RPA | M3 阶段 |

AGNES 网关要点（已核实）：
- Base URL：`https://apihub.agnes-ai.cn/v1`
- 鉴权：`Authorization: Bearer $AGNES_API_KEY`
- 文本：`/v1/chat/completions`（agnes-2.0-flash）
- 图像：`/v1/images/generations`（agnes-image-2.1-flash）
- 视频：`POST /v1/videos` 创建 → `GET /agnesapi?video_id=` 异步轮询（agnes-video-v2.0）

---

## 三、五阶段流水线详解

### 阶段 1 · 选题 · 剧本
- 爆款选题引擎：爬取 / 分析对标账号，提取高完播题材模板。
- 多角色 Agent 编剧（agnes-2.0-flash）：制片人 → 编剧 → 对白润色，输出含爽点节奏的剧本。
- 产出：`script.json`（集数、场景、角色、台词、情绪曲线）。

### 阶段 2 · 分镜 · 角色
- 剧本解析为分镜表：镜头号、景别、运镜、画面描述、台词、时长。
- 角色设定：用 `agnes-image-2.1-flash` 为每主角生成**参考图**（正面 / 半身），**固定 seed 复用**作为一致性锚点。
- 产出：`storyboard.json` + `assets/characters/*.png`。

### 阶段 3 · AGNES 视频生成（核心）
- 每个分镜 = 角色参考图(URL) + 画面描述 + 运镜指令 → 调用 `agnes-video-v2.0` 图生视频。
- 角色一致性：图生视频传 `image=参考图 URL`；多镜头跨场景用**关键帧动画** `extra_body.image=[参考图A, 参考图B]` + `mode=keyframes` 锁主体。
- 异步：先 `POST /v1/videos` 拿 `video_id`，轮询 `GET /agnesapi?video_id=` 取 `remixed_from_video_id`。
- 产出：每镜头 5–18s 视频片段，存 `assets/video/`。

### 阶段 4 · 配音 · 合成
- 台词经 Edge TTS 生成开发用配音（零 key，仅限样片验证；量产商用引擎待定）。
- ffmpeg 合成：视频片段 + 配音 + 字幕（硬 / 软字幕）+ BGM（开源 / 自制）。
- 产出：单集成片 `assets/output/epNN.mp4`。

### 阶段 4 · 配音 TTS 方案（AGNES 网关无 TTS API，音频引擎锁在 Pavo 网页）
- **实测结论（2026-07-07，用老板真实 key）**：AGNES 的 OpenAI 兼容网关 `/v1/models` 仅返回 5 个模型（agnes-1.5/2.0-flash、agnes-image-2.0/2.1-flash、agnes-video-v2.0），**零 audio/TTS**；`/v1/audio/speech` 用任何模型名均返回 `model_not_found` 或 500。→ **API 自动化层面 AGNES 确实调不了配音**。
- **"有的说支持"的来源**：Pavo AI（`app.pavo-ai.work`，新加坡 Agnes AI 旗下一站式网页创作平台）内置 **Agnes-Audio-2.0** 引擎，能做中文「音画实时同步配音」。但 Pavo 是**网页 SaaS、无开发者 API**，只能半自动：AGNES 出视频 → 传 Pavo 网页 → 手动配音 → 下载。
- **结论**：老板"AGNES 有音频能力"对（Agnes-Audio-2.0 确实存在）；但"能当 API 自动调用"不成立（只在 Pavo 网页）。**自动化工作流配音当前采用 Edge TTS（开发验证层）**；量产商用引擎待定（老板 2026-07-07 决定先不管腾讯云，以后需要再说）。

### 阶段 4 · 配音方案（当前：Edge TTS 开发验证层；量产商用引擎待定）

#### 开发验证层 · Edge TTS（微软免费接口，零 key，当前唯一接入的配音引擎）
- **是什么**：`edge-tts` 库调微软 Edge 浏览器内置在线 TTS，**无需 API key、完全免费**，中文神经网络音色（晓晓/云野/云希等）自然度高，短视频圈口碑好。
- **客户端**：`scripts/voice/tts_edge.py`（依赖 `pip install edge-tts`）。已实测连通微软接口并生成 `assets/audio/edge_test.mp3`（21KB）。
- **⚠️ 商用红线**：走微软 Edge **非正式接口**，不在微软正式授权范围内，接口随时可能调整/封禁。**仅用于开发期 / MVP 样片 / 内部验证**，不可量产发片。
- **多角色声线**：用公版不同 VoiceType 音色别名（男主/女主/男配/女配/旁白/少年/古风女），零克隆成本。

#### 量产商用引擎（暂未接入，待量产前拍板）
- 候选已调研：腾讯云 TTS（~¥0.3/集、有授权、国内大厂）、Fish Audio（中文自然+克隆强+商用授权）、火山引擎·豆包（字节系抖音顺）、GPT-SoVITS 本地（零边际成本、需 GPU）。
- `scripts/voice/tts_tencent.py`（腾讯云 TC3 客户端）已写但未启用，届时启用即可。
- **精修备选**：Pavo（Pavo+Agnes-Audio-2.0）网页音画同步配音可作半自动精修，不进自动化核心。

### 阶段 5 · 审核 · 分发
- 自动叠加 AI 生成标识（水印 / 元数据）。
- 平台规则预检（时长、画幅、敏感词）。
- 调用开放平台 API 自动上传，回收播放 / 完播数据回流选题引擎。

---

## 四、角色一致性方案（AGNES 实现）

| 手段 | 说明 |
|---|---|
| 参考图锚定 | agnes-image-2.1-flash 固定 seed 生成每角色 2–3 张参考图，所有镜头复用 |
| 图生视频 | 每镜头传 `image=参考图 URL`，AGNES 视觉一致性保持脸 / 体型 |
| 关键帧动画 | 多镜头用 `extra_body.image=[参考图, 参考图]` + `mode=keyframes` 跨镜头锁一致 |
| 提示词锁定 | 角色外观写成固定 token，避免每镜头重写 |
| 复现 | 固定 `seed`，失败镜头可精确重生成 |
| 产能约束 | VIP 视频约 5 RPM、每日 500 秒，需批次排产（约 30+ 条 15s 镜头 / 天） |

---

## 五、AGNES-AI 最佳实践（研究中，持续补充）

- **图生视频需可公开访问图片 URL**：角色参考图需先落到一个可访问地址（AGNES 图生图返回的 URL 或自有存储），再喂给视频接口。
- **seed 复现**：生成参考图与重生成镜头都用固定 seed，保证一致性可回溯。
- **异步轮询**：视频是异步任务，必须轮询 `video_id` 直到 `status=completed`，拿 `remixed_from_video_id` 才是最终视频。
- **竖屏参数**：9:16 适配抖音 / 快手，对应如 `width:1152, height:2048`（自动映射到 1080p 档）。
- **时长上限**：`num_frames ≤ 441` 且遵循 `8n+1`，`seconds = num_frames / frame_rate`，约 18s 封顶；单集靠多镜头拼。
- **错误与限流**：401 查 key；429 降并发 + 退避重试；503 指数退避。
- **成本**：当前视频 $0/秒（标准 $0.005/秒），先小规模验证。

---

## 六、商用合规清单（必须满足）

- [ ] 角色形象：使用原创 / 授权形象，禁止复刻真人未授权肖像
- [ ] 音色：自训或采购授权音色，禁止克隆他人声音
- [ ] 剧本：原创或已取得改编权，规避侵权
- [ ] BGM：使用开源 / 商用授权音乐（如 Pixabay、Epidemic 商用版）
- [ ] AI 标识：成片含平台要求的「AI 生成」声明
- [ ] 平台规则：过抖音 / 快手 / 视频号内容审核，规避敏感题材
- [ ] 数据合规：账号与发布数据归老板所有，脚本本地留存

---

## 七、实施路线图

| 阶段 | 目标 | 交付 |
|---|---|---|
| M1（本周） | 最小闭环跑通 | AGNES 剧本→参考图→单镜头视频→合成，出 1 条样片 |
| M2（2–3 周） | 批量生产 | 角色一致性打磨 + 多集批量 + 配音接入 |
| M3（1 月） | 全自动分发 | 审核水印 + 三平台自动上传 + 数据回流 |
| M4（持续） | 商用闭环 | 合规审计 + 题材模板库 + 成本优化 |

---

## 八、需老板提供的密钥（已建 `.env.example`）

- `AGNES_API_KEY`：AGNES-AI VIP 网关密钥（视频 + 图像 + 文本统一）
- 配音（当前用 Edge TTS，零 key）：无需任何密钥配置；量产商用引擎待定，候选含腾讯云 TTS / Fish Audio / 火山 / GPT-SoVITS 本地（以后需要再接入）。
- 分发凭证：抖音 / 快手开放平台 client id / secret（M3 阶段用）
- 一台可跑 ffmpeg + Python 的机器（本地或云 ECS）

> AGNES 当前视频 $0/秒，成本可控。密钥填好即可启动 M1。
