# P1 + Thinking 开发规格（稳定商业化 · 第一批）

> 日期：2026-08-11 ｜ 依据：整合方案 Phase 1 四件 + Thinking 模式（老板拍板全做）
> 原则：**按官方来，先稳定**。每项 = 功能改动 + UI 改动 + 测试流程 + 验收标准。
> 基线：`_archive_20260811_stable_v1/`（改动前快照已存）。

---

## T1 · 图生视频稳定性句（官方图生视频结构）

**背景**：官网图生视频提示词结构 = "描述什么该动 + 哪些保持稳定"（官方例子 `...while keeping the face and outfit consistent`）。我们 reference 分支目前只有场景描述+身份锁，未用官方"稳定性句"。

**功能改动**（server.py，约 5 行）：
- `generate_video_real` reference 分支，拼完 identity_lock 后追加官方稳定性句：
  `while keeping the face and outfit consistent, stable character identity throughout`
- 仅当 `ref_key`（有角色引用）时追加；空镜（无 ref）不加（无脸可锁）。
- keyframes 分支**不加**（官方关键帧模式本身是"过渡描述+保持身份"，已覆盖）。

**UI 改动**：无。

**测试流程**：
1. 内存单测：reference 分支（有 ref）→ prompt 含稳定句；keyframes 分支 → 不含；空镜 reference → 不含。
2. 真实链路：⑤ 对 1 个 reference 角色镜「保存并重渲」，看日志/结果 ok。

**验收标准**：
- [ ] reference 角色镜最终 prompt 含 `while keeping the face and outfit consistent`
- [ ] keyframes / 空镜不受影响（不含稳定句）
- [ ] 生成结果 ok，无新增报错

---

## T2 · 分镜源头带 scene_type（替代事后规则分类）

**背景**：官方叙事层 `narrative.plan_novel` 是在分镜生成时标注 scene_type；我们现在是生成后 `_classify_scene_type` 规则猜（准度有限）。源头模型直接输出最准。

**功能改动**（server.py）：
- `STORYBOARD_SYS` shots schema 加字段：
  `"scene_type": str(action|monologue|dialogue_2|dialogue_multi)`
  + Rules 补一条：每镜按内容标注 scene_type（内心独白/旁白=monologue；有台词对话=dialogue_2；多人/群戏=dialogue_multi；其余=action）。
- `load_spec` 兼容：shot 无 `scene_type`（旧数据）→ `_classify_scene_type` 兜底写回（已有逻辑，改为"仅缺失时调用"）。
- `_classify_scene_type` 改为：`shot.scene_type` 已存在且合法 → 直接复用；否则规则分类。

**UI 改动**：无（数据层）。

**测试流程**：
1. 真实 AGNES 生成一次分镜（直调 chat+STORYBOARD_SYS）→ 检查 shots 全部带 scene_type 且与内容匹配（有台词镜=dialogue_2、空镜=action）。
2. 旧项目（老板 212526 无 scene_type）加载 → 不崩、兜底分类写回。

**验收标准**：
- [ ] 新生成分镜 100% 带 `scene_type`，抽查 3 镜分类与内容一致
- [ ] 旧项目加载零报错，缺失镜自动兜底分类
- [ ] 不影响分镜 JSON 其他字段

---

## T3 · scene_type 徽章 + 运镜预设 chips（UI）

**背景**：scene_type 已入数据层，但前端不显示（看不出这镜用哪套电影语法）；运镜入口在删三栏后只剩手输框（官方 14 运镜词库没用上）。

**UI 改动**（studio.html，纯前端）：
- ⑤ 卡片信息行（`镜 #N` 行）加 scene_type 徽章：
  - `monologue`→🎭独白（紫）、`dialogue_2`→💬对话（青）、`dialogue_multi`→👥群戏（橙）、`action`→🎬动作（灰）
  - 前端 `sceneTypeMeta(sh)` 方法映射；缺省兜底 `action`。
- ⑤ 运镜输入框下加官方 14 运镜 chips 一行（推/拉/跟/横移/摇/升降/手持/荷兰角/环绕/变焦/过肩/主观/甩镜/固定），点击 → `sh.camera = 该词`。
  - 复用已有 `cameraEn` 翻译表（前端），加 `cameraPresetsList` 常量数组。

**功能改动**：无（后端已写 scene_type；chips 只是填输入框，保存走现有 v-model+putSpec）。

**测试流程**（Playwright）：
1. 加载老板项目 → 切⑤ → 断言卡片出现 scene_type 徽章（至少 1 镜显示"对话"或"动作"）。
2. 点击"环绕"chips → 断言该镜 `sh.camera === '环绕'` 且输入框值更新。
3. 控制台零 JS 错误。

**验收标准**：
- [ ] 每张卡片显示 scene_type 徽章（文案+颜色正确）
- [ ] 14 个运镜 chips 渲染；点击后运镜框填值
- [ ] 零 JS 错误，不破坏现有布局（左图右文）

---

## T4 · 视频成片尺寸回读

**背景**：官网明确"成片尺寸以响应 `size`/`metadata.size_mapping` 为准"（提交 720×1280 会被归一化到 480p/720p/1080p 档）。合成阶段需要真实尺寸。客户端 `_await_video` 只返回 URL，最可靠途径 = 本地化后 ffprobe 实测。

**功能改动**（server.py）：
- `generate_video_real` 视频本地化成功后（`fetch_binary` 写盘），用 `ffprobe`（subprocess，已有 ffmpeg 环境）读 `width`/`height`/`duration`：
  - 成功 → 写回 `shot["video_size"] = "WxH"`、`shot["video_seconds"] = 秒数`，`_save_spec()`。
  - ffprobe 缺失/失败 → 静默跳过（不阻断，不报错）。
- 结果 dict 带 `size`/`seconds` 字段。

**UI 改动**（studio.html）：
- ⑤ 引擎参数区（⚙️ 折叠）"尺寸"行：有 `sh.video_size` 时显示 `成片 WxH`（替代/并列 720×1280 预设）；有 `video_seconds` 显示实际秒数。

**测试流程**：
1. 内存单测：ffprobe 解析函数对样例输出正确解析。
2. 真实链路：生成/重渲 1 镜 → 断言 `shot.video_size` 非空且与本地文件 ffprobe 一致。
3. 前端：引擎参数区显示成片尺寸。

**验收标准**：
- [ ] 成片后 `shot.video_size`/`video_seconds` 落盘且与文件实测一致
- [ ] 引擎参数区显示"成片 WxH"
- [ ] ffprobe 缺失时不报错、不阻断生成

---

## T5 · Thinking 模式（源头生成质量）

**背景**：官网 agnes-2.0-flash 支持 Thinking（`chat_template_kwargs.enable_thinking` / `thinking.type=enabled` + `budget_tokens`），复杂任务（剧本/分镜）质量可提升。当前 chat() 未暴露。

**功能改动**（agnes_client.py + server.py）：
- `agnes_client.chat()` 加参数 `thinking=False, budget_tokens=None`：
  - `thinking=True` → body 加 `"chat_template_kwargs": {"enable_thinking": True}`（OpenAI 兼容格式，官网示例）。
  - 不影响现有调用（默认 False，body 不变）。
- server.py 分镜生成（STORYBOARD_SYS 的 chat 调用）→ `thinking=True, budget_tokens=2048`；
  大纲生成（OUTLINE_SYS）→ 同样开启；
  需求卡/交互类 → 保持 False（快）。
- 分镜生成的 max_tokens 已在 8000，thinking 额外消耗算在 output budget 内——若响应变慢可接受（质量优先）。

**UI 改动**：可选增强——分镜/大纲生成时 busyLabel 显示「深度思考中…」（复用现有 busyLabel 机制，非必须）。

**测试流程**：
1. 内存：chat body 组装含 `chat_template_kwargs.enable_thinking`（thinking=True 时）。
2. 真实调用：分镜生成 1 次（thinking on）→ 正常返回合法 JSON、shots 完整、scene_type 字段带出（与 T2 联测）。
3. 回归：需求卡生成（thinking off）→ 行为不变、延迟正常。

**验收标准**：
- [ ] thinking=True 请求体含 enable_thinking 且请求成功
- [ ] 分镜生成（thinking）返回合法 JSON，质量/结构不劣于关闭时
- [ ] 非 thinking 调用（需求卡等）零影响

---

## 总回归流程（5 项一起验收）

1. **语法**：`py_compile server.py agnes_client.py` + node `--check` studio.html JS。
2. **内存单测**：T1 稳定句 / T2 分类兼容 / T4 ffprobe 解析 / T5 body 组装（不烧 token）。
3. **干净重启**：杀 8777 全部实例（含僵尸）→ 新进程绑定 → 8787 正常。
4. **Playwright 冒烟**：⑤ 卡片徽章 + chips + 引擎参数区，零 JS 错误；旧项目加载不崩。
5. **真实链路 1 镜**：重渲 1 个 reference 角色镜 → prompt 含稳定句、成片尺寸回读。
6. **产出**：全部通过 → 汇报 + 老板强刷验收。

## 总验收（老板视角）

- 刷新后 ⑤ 卡片可见 scene_type 徽章 + 运镜一键 chips
- 重渲 1 镜：参考图视频更贴官方（稳定句）、成片后引擎参数区显示真实尺寸
- 重新生成分镜：每镜自带 scene_type（源头）
- 出片链路 ①→⑥ 照常跑通
