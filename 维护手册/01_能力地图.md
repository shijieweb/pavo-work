# 01 · 能力地图（UI ↔ 后端 ↔ 真实状态）

> 每个能力：UI 操作 → 后端 API → 状态。✅=真实链路；⚠️=部分/暂缓。

## ① 创意·需求卡（story）

| 能力 | UI 操作 | 后端 API | 状态 |
|---|---|---|---|
| 贴小说生成需求卡 | 🤖 快速生成 → 采纳 | `/api/agent`（异步 task 轮询） | ✅ |
| 一句话主题 → AI 写小说 | 💡 一句话主题 | `/api/novel/generate` | ✅ |
| 确认需求卡 | ✅ 确认需求 | `/api/meta`（req_card） | ✅ |
| 需求卡草稿 | 自动存 localStorage | — | ✅ |

## ② 剧本大纲（outline）

| 能力 | UI 操作 | 后端 API | 状态 |
|---|---|---|---|
| 生成大纲（thinking） | 🤖 生成大纲 | `/api/outline/generate` | ✅ 自动保存 meta |
| AI 英文关键词 | 🤖 AI 生成英文关键词 | `/api/style/keywords` | ✅ |
| 确认 → 生成分镜 | ✅ 确认大纲 | `/api/generate/storyboard`（thinking） | ✅ 空/shots 空自动重试 3 次 |

## ③ 资产设计（image）

| 能力 | UI 操作 | 后端 API | 状态 |
|---|---|---|---|
| 中文描述 → 英文提示词 | 🤖 生成英文提示词 | `/api/asset/prompt` | ✅ |
| 生成图片（单/全量） | 🔄 生成图片 / ⚡ 一键生成全部 | `/api/asset/prompt` + 图生成 | ✅ confirm 防误触 |
| 提取全部中文 | 🤖 提取全部中文 | `/api/asset/extract` | ✅ style-only 空壳判定已修 |
| cn_prompt 源头自带 | 分镜生成时 schema 输出 | 分镜生成 | ✅ 0810 起 |

## ④ 分镜校准·关键帧（video）

| 能力 | UI 操作 | 后端 API | 状态 |
|---|---|---|---|
| 批量生成全部首尾针 | 批量按钮 | `/api/generate/keyframes/shot`（提交+轮询 status） | ✅ 幂等跳过/429 重试 |
| 单镜重生成 | 🔄 重生成 | 同上 + force | ✅ |
| AI 优化首尾针提示词 | 🤖 AI优化 | `/api/prompt/optimize` | ✅ |
| 引用素材下拉 | 首针参考图行 | PUT spec（@change 即落盘） | ✅ |

## ⑤ 分镜视频（shots）

| 能力 | UI 操作 | 后端 API | 状态 |
|---|---|---|---|
| 生成视频 / 保存并重渲 | 卡片按钮 | `/api/generate/shot`（提交+轮询 status） | ✅ 524 免疫/幂等/force |
| 批量生成全部视频 | 批量按钮 | 同上并发（组间并行/组内串行） | ✅ |
| 光线/风格 select | 卡片右列 | 随 payload.shot 提交 | ✅ |
| 运镜 chips（官方 14 种） | 点击填入 | 同上 | ✅ |
| 最终提示词实时拼装 | 🎬 折叠区 | 前端模拟（与后端一致：清洗/翻译/去重） | ✅ |
| 引擎参数区 | ⚙️ 折叠区 | 展示 + negative_prompt 可编辑 | ✅ |
| AI 诊断（4 维） | 🧠 AI 诊断 | `/api/diagnose`（AGNES 多模态抽帧） | ✅ 写回 shot.diagnosis |
| 人脸一致性质检 | 👤 面板 | `/api/faceqc` + `/api/facefix`（SFace 本地） | ✅ 0811 补白名单 |

## ⑥ 合并成片（advanced）

| 能力 | UI 操作 | 后端 API | 状态 |
|---|---|---|---|
| 合成成片（转场+BGM+字幕+AI标识） | ▶ 合并全部分镜 | `/api/finalize` → assemble.py（--resolution 随档位） | ✅ |
| 合成检查表 | 静态显示 | — | ✅ 0811 动态化 |
| 成片质检 | 🔬 运行质检 | `/api/quality`（本地 ffmpeg 免额度） | ✅ 0811 挂 UI |
| 导出成片 / 打包工程 | ⬇ 导出 / 📦 ZIP | `/api/export` | ✅ |
| 分发（多平台） | 文案"导出 / 分发" | 后端 distribute() 有，前端无入口 | ⚠️ 老板明确暂缓 |
| 修复分镜↔参考图关联 | 按钮 | `/api/project/reconcile` | ✅ |

## ⚙ 设置（settings）

| 能力 | UI 操作 | 后端 API | 状态 |
|---|---|---|---|
| 生成参数：分辨率 test/prod + 画幅 横/竖 | 🎬 生成参数下拉 | `/api/meta`（resolution_mode/aspect_mode） | ✅ |
| 提示词库编辑 | 保存提示词库 | `/api/prompt/library` | ✅ |
| 密钥池状态 | 刷新 | `/api/key-pool` | ✅ |
| 任务队列（O3） | 刷新/暂停/取消 | `/api/queue/*`（BATCH_QUEUE 调度器） | ✅ |
| 启动批量流水线（夜间队列） | 按钮 | `/api/pipeline/batch` | ✅ |
| 一键全自动 | 🚀 按钮（底部） | `/api/pipeline/run` + progress | ✅ |
