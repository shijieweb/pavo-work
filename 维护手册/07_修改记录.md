# 07 · 修改记录（倒序）+ 已知问题

> **加需求/改功能前先看这里**，避免重复造轮子；改完必登记（日期 | 改动 | 验证）。

## 修改记录

| 日期 | 改动 | 验证 |
|---|---|---|
| 0811 | **运镜镜头首帧改造**（老板洞察）：源头 STORYBOARD_SYS 强制运镜镜头输出 first_frame_prompt（起点画面）+ RULES 8.5；后端 `_is_camera_move`（36 中英文运镜关键词+手动三态）+ `_gen_camera_start`（场景图/锚点底图 → i2i，无底图纯文生，本地转 data URI）；主流程接入（链式→运镜起点→锚点→文生兜底）；前端 ④模块运镜徽标🎥+三态开关+label 动态；库文件同步 6228 字符 | 判定 8/8 + 链路 4/4：运镜镜首帧=咖啡店过道起点画面（camera-start），固定镜 chain 链式，零 JS 错误 |
| 0811 | **08_经验避坑库** 新建（老板：经验必沉淀）+ 00 导航 + 本表 + 04 管线同步 | 全量 40+ 条经验归类 |
| 0811 | README 门面 + 免费 key 双模式配置（AGNES_TEST_API_KEY 实测 8.4s）+ **git push 根因修复（沙箱拦截 git smart HTTP→老板放行）** + 仓库公开（私有化触发=3 集商业化标准） | 非沙箱 push 成功 c3ba5b0..57a9c82，gh 双确认 |
| 0811 | 开发流程落地：key 双模式（AGNES_TEST_API_KEY 自测免费/VIP 正式）+ AGNES 原始响应环形缓存(/api/agnes/last) + health_check.py + qa_regression.py(fast 11项) + 协作规范文档 + git 建仓(2 commit) | fast 11/11 全绿 |
| 0811 | ⑥检查表动态化（genSizeLabel/AI 标识随 finalUrl）；⑤人脸面板补回；⑥成片质检+⑤AI 诊断挂 UI；faceqc/facefix 补代理白名单 | 真实质检 fail(13 静音)/人脸 9 镜/零 JS 错误 |
| 0811 | 三流程真实测试（横屏全链路/竖屏参数操控/批量+合成）→ 修复：分镜空重试、scenes dict→list、newProject 清 reqCard、emotion 翻译、合成分辨率随档位 | 17/17→15/15→10/10 |
| 0811 | 测试/正式分辨率 + 横屏支持（_video_size/aspect_mode/cinema 画幅词） | 三态单测 + 真实 448×832 / 832×448 |
| 0811 | P1+Thinking（稳定句/scene_type 源头/徽章+chips/尺寸回读/thinking） | 三轮真实自测全绿 |
| 0810 | 全流程真实走查（清历史→①→⑥ 成片）→ 修复 429 重试 / data URI / load_spec scene_type 兜底 | 各模块 10-16 项全绿 |
| 0810 | 全状态记忆（workspace_state A 类服务端 / localStorage B 类草稿） | 三轮 21/21 |
| 0810 | 日志系统（按天滚动/前端上报/logs.html/WARNING 模式 403） | 全链路验证 |
| 0810 | 全首尾针管线（链式/跳场景/文生兜底/幂等） | 链式单测 4/4 |
| 0810 | 视频 524 免疫（提交+轮询）+ 并发批量 + 生成中遮罩 | 状态机验证 |
| 0810 | 资产中文描述源头生成（schema 加 cn_prompt） | 真实生成验证 |
| 0810 | 需求卡/大纲不持久化修复（生成即保存+localStorage 双保险） | 兜底恢复验证 |

## 已知问题 / 债务

| 项 | 说明 | 状态 |
|---|---|---|
| 分发未实现 | 后端 distribute() 在，前端无入口；⑥文案含"分发"字样 | 老板明确暂缓 |
| /api/series/ 无前端入口 | 跨集锁脸能力后端有、UI 未暴露 | 🔲 待接 |
| 批量视频依赖浏览器 | 后端无独立循环触发批量；浏览器关则提交中断（已提交任务不受影响） | 🔲 待改 |
| 429 重试未专门回归 | 逻辑已加，未做专项三轮 | 🔲 待补测 |
| 老板项目 012147 的 final.mp4 是旧 1080 版 | 质检报 13 段静音；可重新合 480p 测试版对比 | 待老板 |
| P2 能力未做 | 多图合成双人锚点 / 资产自然语言修改 / 过审洗稿 / SSIM 闸门 / 正反打 | 🔲 规划 |

## 维护流程规范（老板定）
1. 老板提需求 → **先看 00 总览定位分册** + 07 修改记录查重
2. 评估影响面 → 07 登记一行
3. 改代码（studio.html / server.py / agnes_proxy.py 按需）
4. 按 05 测试指南验证（含三轮）
5. 更新对应分册 + 07 记录 → 汇报
